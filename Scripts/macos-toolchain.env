# One CI compiler pin for normal builds and exact-tag release verification.
# Local builds may use a newer full Xcode; preflight checks Package.swift's
# required Swift version and records the actual toolchain in every run.
LOCALFLOW_CI_XCODE_VERSION=26.3
